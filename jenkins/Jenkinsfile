pipeline {
  agent { 
        docker { 
           image 'maven:3.8.7-eclipse-temurin-11'
           arg '-v /root/.m2:/root/.m2'
           }
   }
   stages {
        stage('BUILD') {
            steps {
              sh 'mvn -B -DskipTests clean package'
             }
         }
      }
}
